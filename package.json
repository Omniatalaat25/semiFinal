{
  
  "devDependencies": {
    "json-server": "^0.17.0",
    "serve": "^11.2.0"
  },
  "main": "index.html",
  "name": "swiper-slides-per-group-skip",
  "scripts": {
    "db":"npx json-server --watch data/db.json",
    "build": "echo This is a static template, there is no bundler or bundling involved!",
    "start": "serve"
  },
  "tags": [
    "swiper"
  ]
}
